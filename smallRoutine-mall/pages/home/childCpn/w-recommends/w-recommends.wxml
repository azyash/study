<!--pages/home/childCpn/recommends/recommends.wxml-->
<view class="recommends">
  <block wx:for="{{recommends}}" wx:key="index">
    <view class="recommends-item">
      <image src="{{item.image}}" bind:load="handleImageLoad"/>
      <view>{{item.title}}</view>
    </view>
  </block>
</view>
